# Transitive deps of pi-coding-agent ship build scripts this package does not need; deny them explicitly so pnpm 11 stops complaining before every script run
allowBuilds:
  "@google/genai": false
  esbuild: false
  protobufjs: false
